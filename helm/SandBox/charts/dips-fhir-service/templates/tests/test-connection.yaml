apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dips-fhir-service.fullname" . }}-test-connection"
  labels:
    {{- include "dips-fhir-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dips-fhir-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
