apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "growth-chart-app.fullname" . }}-test-connection"
  labels:
    {{- include "growth-chart-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "growth-chart-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
