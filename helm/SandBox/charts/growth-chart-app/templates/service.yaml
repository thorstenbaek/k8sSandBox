apiVersion: v1
kind: Service
metadata:
  name: {{ include "growth-chart-app.fullname" . }}
  labels:
    {{- include "growth-chart-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "growth-chart-app.selectorLabels" . | nindent 4 }}
